<?xml version="1.0" encoding="UTF-8"?><beans xmlns="http://www.springframework.org/schema/beans"       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"       xmlns:mvc="http://www.springframework.org/schema/mvc"       xmlns:aop="http://www.springframework.org/schema/aop"       xmlns:context="http://www.springframework.org/schema/context"       xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.3.xsd   http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-4.3.xsd        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">    <mvc:annotation-driven/>    <!-- 自动扫描且仅扫描@Controller，不要扫描其他注解，以免和spring基本配置的其他扫描冲突 -->    <context:component-scan base-package="com.ljy.maven_github_ssm.web"                            use-default-filters="false">        <!-- use-default-filters="false"表示不适用默认的过滤器，而是自己的过滤器 -->        <!-- 表示只扫描@Controller的注解-->        <context:include-filter type="annotation"                                expression="org.springframework.stereotype.Controller"/>        <context:include-filter type="annotation"                                expression="org.springframework.web.bind.annotation.ControllerAdvice"/>    </context:component-scan>    <!-- 视图解析器 -->    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">        <property name="prefix" value="/"/>        <!--  <property name="suffix" value=".jsp"></property> -->    </bean>    <!-- 加入该注释就可以访问静态文件 -->    <mvc:default-servlet-handler/>    <!-- 拦截器组 -->    <mvc:interceptors>        <!-- 针对后台的Admin账户的拦截器 -->        <mvc:interceptor>            <!--拦截的目录包括  /**的意思是所有文件夹及里面的子文件夹 -->            <mvc:mapping path="/backstage/**"/>            <!--不拦截的目录 -->            <mvc:exclude-mapping path="/backstage/toLogin" />            <mvc:exclude-mapping path="/backstage/login" />            <mvc:exclude-mapping path="/backstage/logout" />            <!--jsp里引入了css、js等文件也会被拦截，因此这里设置为不拦截的css和js文件 -->            <mvc:exclude-mapping path="/**/*.css"/>            <mvc:exclude-mapping path="/**/*.js"/>            <mvc:exclude-mapping path="/**/*.png"/>            <mvc:exclude-mapping path="/**/*.jpg"/>            <mvc:exclude-mapping path="/**/*.gif"/>            <!-- 登录验证拦截器，用于拦截登录请求 -->            <bean id="loginForAdminInterceptor" class="com.ljy.maven_github_ssm.web.filter.LoginForAdminInterceptor"/>        </mvc:interceptor>    </mvc:interceptors></beans>